SNMP Manager + Web APP 

Дипломная работа Грачева Дениса.
На тему: "Мониторинг ЛВС по протоколу SNMP"

Deamon (C++)
Набор сервисов:
1) Инициализация данных из бд(устройства, поддерживаемые им комманды и т.п.).
2) Инициализация сервера, способного принимать подключение по ip. (Для консольного общения с приложением).(не полностью)
3) Аудентификация.
4) При запуске переход в фоновый режим.
5) Работа с бд (чтение/запись)
6) Логирование (не полностью)

Web APP (Ruby on Rails)
Набор сервисов :
1) Аудентификация. 
2) Управление данными об устройствах
3) Возможность просмотра статистики по отдельным устройствам (как в виде графика, по возможности, 
   так и в виде списка).
4) Генерация отчета по отдельным запросам для устройства. (запрос возможен вручную через браузер (snmpget, snmpwalk))
5) Управление списком комманд для устройства по которым необходимо собирать информацию.
6) Управление таймингами для каждого устройства. (Можно управлять интервалами между запросами к каждому устройству,
   но не каммандами отдельно)

Стороние библиотеки и инструменты:
1) sqlite3 (тестировалось на 3.8.2).
2) bcrypt (исходники есть в ./general/utils/bcrypt/).
3) net-snmp (5.7.2.1).
4) ruby >=2.0.0.
5) Rails 4.1.7 (набор необходимых гемов приведен соответстветнно в Gemfile).


Нету значения что запускать первым, так как оба приложения 
не зависимы друг от друга и работают независимо. 
Т.е. Запускаете любое, оно создает/подключается к бд,
второе спокойно подключается к уже существующей.

П.С. При запуске из под демона db инициализируется в асинхронном режиме. 

П.С.2 Production deamon  - ./WebServer/general
      Development deamon - ./general
      WebApp - ./WebServer 
